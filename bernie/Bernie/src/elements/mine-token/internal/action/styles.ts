import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    margin: 5,
    padding: 10,
    backgroundColor: 'green',
    borderRadius: 10,
    alignSelf: 'center',
  },
});
