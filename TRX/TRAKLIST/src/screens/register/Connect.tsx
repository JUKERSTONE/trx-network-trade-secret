import React from 'react';
import {View, Button} from 'react-native';
import {ConnectContainer} from '../../container';

export const ConnectScreen = ({...props}) => {
  return <ConnectContainer {...props} />;
};
